/// @description Normalize the data
// You can write your code in this editor

total_distance = global.checkpoint_frames / (global.checkpoint_frames / 100);
speed_rate = (global.scroll_speed / 4) / 60;

boost = 0;

move_rate = speed_rate + boost;